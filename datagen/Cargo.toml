[package]
name    = "datagen"
version = "0.1.0"
edition = "2021"

[dependencies]
engine = { path = "../engine" }
chess  = { path = "../chess" }
utils  = { path = "../utils" }
montyformat = "0.9.2"
crossbeam = "0.8"
rand = "0.9"

[features]
datagen = ["engine/datagen"]