pub struct UciProcessor;
impl UciProcessor {

    //Handles commands from UCI protocol
    pub fn execute( command: &str, args: &[String] ) {
        match command {
            _ => return
        }
    }
}