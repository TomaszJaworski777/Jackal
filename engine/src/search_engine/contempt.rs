use crate::search_engine::engine_options::EngineOptions;

#[derive(Debug, Clone, Copy)]
pub struct Contempt {
}

impl Contempt {
    pub fn rescale(win_lose_delta: &mut f64, draw_chance: &mut f64, sign: f64, invert: bool, options: &EngineOptions) {
    }
}